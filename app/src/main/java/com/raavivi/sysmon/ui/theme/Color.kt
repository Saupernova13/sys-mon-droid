package com.raavivi.sysmon.ui.theme

import androidx.compose.ui.graphics.Color

// Dark dashboard palette — slate surfaces with a mint/teal accent, echoing the
// sys-mon web UI rather than the default Material purple.
val Mint = Color(0xFF3DDC97)
val MintDim = Color(0xFF2BB37A)
val Sky = Color(0xFF5AC8FA)
val Amber = Color(0xFFF5B642)
val Coral = Color(0xFFE2575B)
val Violet = Color(0xFF9D8CFF)

val BgDark = Color(0xFF0E1116)
val SurfaceDark = Color(0xFF161B22)
val SurfaceVariantDark = Color(0xFF1E2630)
val OutlineDark = Color(0xFF2C3542)
val OnSurfaceDark = Color(0xFFE6EDF3)
val OnSurfaceMuted = Color(0xFF8B98A5)

// Per-metric accent colours used by the cards + charts.
val CpuColor = Mint
val RamColor = Sky
val GpuColor = Violet
val DiskColor = Amber
